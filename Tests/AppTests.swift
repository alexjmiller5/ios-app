import XCTest
@testable import CHANGEME

final class AppTests: XCTestCase {
    // Replace with real tests for the app's core logic (TDD: write these first).
    func testScaffold() {
        XCTAssertTrue(true)
    }
}
